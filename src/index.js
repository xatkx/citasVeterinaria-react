import React from 'react';
import ReactDOM from 'react-dom/client'
import './index.css'

import App from './App' // main components
ReactDOM.createRoot(document.querySelector('#root'))
.render(
    <React.Fragment>
        <App />
    </React.Fragment>
)